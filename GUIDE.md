# wholesale-agent 六步开发路线

> 目标：从零搭一个批发业务 Agent，**每一步都是可运行的增量**，跑通一步再进下一步，吃透企业 Agent 的完整构成。
>
> 对应的最终形态：知识型问答（RAG）+ 数据型问答（读工具）+ 动作执行（写工具 + 人审）+ 流程编排（工作流）。

## 总览

| 步骤 | 加什么能力 | 新增文件 | 你会看到的质变 |
|------|-----------|---------|--------------|
| Step 1 | 纯对话基线 | （已就绪） | 会聊，但没有数据和知识 |
| Step 2 | 第一个读工具 | `data/customers.json`、`lib/tools.ts` | "查张三欠款"从编造变真数据 |
| Step 3 | 工具矩阵 + 分流 | `lib/tools.ts` 扩充 | 多个工具并存，LLM 自己选对 |
| Step 4 | RAG 知识库 | `knowledge-base/*.md`、`lib/knowledge.ts` | "退货政策"从瞎答变检索 |
| Step 5 | 双通道汇合 | 改造 `route.ts` | 混合问题"欠多少怎么催"两路并用 |
| Step 6 | 写工具 + 人审 | `lib/tools.ts` 加写工具 | 从"只会查"到"能干活"，但必须人审 |

---

## Step 1 · 纯对话基线（当前状态）✅

**目标**：跑起来一个"批发顾问"聊天页面，确认地基没问题。

**跑法**：
```bash
cd wholesale-agent
npm run dev
```
打开 http://localhost:3000

**验证 4 个示例问题**（首页就有快捷入口）：
1. "你能做什么？" → 应如实说明能力边界
2. "批发业务里账期一般怎么定？" → 能聊行业方法论
3. "查一下张三欠了多少货款" → **应承认没有查询工具**（Step 2 要消灭的无能）
4. "咱们公司的退货政策是什么？" → **应承认没接制度文档**（Step 4 要消灭的无能）

**认知点**：
- `streamText`：流式调 LLM 的标准姿势
- `system prompt` = Agent 的人设 + 能力边界声明
- **基线的价值**：记住 3、4 两题它答不上来的样子，后面每步回来重问，亲眼见证 Agent 变强

---

## Step 2 · 第一个读工具

**目标**：加 `getCustomerDebt` 工具 + mock 欠款数据，让 Agent 能真查数据。

**新增**：
- `data/customers.json` — 模拟订单豹风格的客户欠款数据（客户名、欠款金额、账龄、最后跟进时间）
- `lib/tools.ts` — 第一个 `tool()`：`getCustomerDebt(customerName)`
- `route.ts` 加 `tools` + `maxSteps`，观察工具调用循环

**验证**：重问"查一下张三欠了多少货款" → 回答带真实 mock 数字和账龄。

**认知点**：
- `tool()` 三要素：`description`（给 LLM 看的菜单）、`parameters`（zod schema）、`execute`（真实执行）
- Function Calling 循环：LLM 决定调工具 → 拿到结果 → 继续生成
- **description 写得好不好，直接决定 LLM 调不调这个工具**

---

## Step 3 · 工具矩阵与分流

**目标**：再加 `searchStock`（查库存）、`getOrders`（查订单）等读工具，观察 LLM 在多工具下自己选对。

**验证**：问"张三欠多少"、"A 商品还有多少库存"、"上个月谁的订单最大" → 各自命中不同工具，不用你写任何 if-else。

**认知点**：
- 工具越多，description 的区分度越重要（两个描述模糊的工具，LLM 会调错）
- 这就是"问题分流"的真实实现：**分流逻辑住在 LLM 脑子里，不在你的代码里**

---

## Step 4 · RAG 知识库

**目标**：接入进销存制度文档，"退货政策"类问题走检索。

**新增**：
- `knowledge-base/` — 退货政策、账期规则、客户等级制度等 .md 文档
- `lib/knowledge.ts` — 切分 + 硅基流动 bge-m3 向量化 + 相似度检索（my-first-agent 已跑通的模式，这里自己再写一遍）
- `route.ts` 检索结果拼进 system prompt

**验证**：重问"退货政策是什么" → 回答出自你的文档，不再是通用知识。

**认知点**：
- RAG 管静态知识、工具管活数据——两类信息的物理存放位置不同，接入方式就不同
- 前置式检索（每条消息都查）vs 智能分流式的取舍

---

## Step 5 · 双通道汇合

**目标**：验证混合问题"张三欠了多少，按公司政策该怎么催？"能同时用上工具数据 + RAG 文档。

**验证**：问混合问题 → 回答既有真实欠款数字（来自工具），又引用了催款政策（来自文档）。

**认知点**：
- 两路并列、结果汇合，不是串联
- 企业 Agent 的核心形态：**多信源证据 + LLM 综合**——这步跑通，你就理解了"企业级"和"聊天机器人"的本质区别

---

## Step 6 · 写工具与人审

**目标**：加 `sendDunningMsg`（发催款消息）写工具，但 execute 只返回"已生成草稿，等待人工确认"，不真发。

**验证**：问"帮我催一下张三的欠款" → Agent 生成话术草稿 + 明确标注"待人审"，不直接执行。

**认知点**：
- 读工具无副作用可放开；写工具有副作用必须人审——这是企业敢用 Agent 的底线
- "Agent 建议 + 人确认"是企业落地的标准起步形态
- （更后面）多个步骤串成流程 = LangGraph.js 工作流，独立阶段再学

---

## 环境说明

- 依赖：Next.js 14.2.5 + Vercel AI SDK 3.3.0 + DeepSeek（对话）+ 硅基流动 bge-m3（Step 4 起）
- `.env` 已配好两个密钥；Step 1 只用 DeepSeek
- node_modules 从 my-first-agent 复制而来，依赖完全一致，直接 `npm run dev`
