# sirkaikai.cn 绑定 wholesale-agent 操作清单

## 现状
- 域名 `sirkaikai.cn` 已在**腾讯云**购买（同家，备案最顺）
- 目标：让 `sirkaikai.cn` 永久加载 wholesale-agent，替代 EdgeOne 默认域的 3 小时预览限制
- 当前部署：EdgeOne Pages（wholesale-agent.edgeone.cool）

## 步骤总览
买域名(已完成) → 实名 → 备案(1-2周) → 绑 EdgeOne → DNS 解析 → 生效

---

## 1. 确认域名实名（.cn 必须）
腾讯云控制台 → **域名注册** → 我的域名 → `sirkaikai.cn` → 看「实名认证」状态
- 已实名 → 跳过
- 未实名 → 按提示传身份证，1-3 天审核

## 2. ICP 备案（接入方锁定腾讯云）
腾讯云控制台 → 搜 **网站备案** → 进备案系统
- 选「新增网站备案」（个人/企业主体）
- 主体信息：个人填身份证；企业填营业执照 + 法人
- 网站信息：
  - 域名：`sirkaikai.cn`
  - 服务/接入：选腾讯云资源（EdgeOne 节点归属腾讯云）
  - **注意：备案系统通常要求选择「云服务器/轻量应用服务器」实例。若你只有 EdgeOne Pages，需先买一台最便宜的轻量应用服务器当备案「壳」**
  - 接入服务商：腾讯云
- 传证件照 → 提交
- 腾讯云初审(1-2天) → 管局终审(约 1-2 周)
- 备案号下来后，域名才能在国内正常解析到 EdgeOne

### 备案服务器成本与省钱方案
- 当前腾讯云最便宜的轻量应用服务器（2核2G）约 **¥336.96/年**（以购买页实时价格为准）
- 这台服务器只是满足备案表单要求，不跑网站，站点仍由 EdgeOne Pages 承载
- 省钱选项：
  1. **等活动/秒杀/新客特惠**：价格可能降到 ¥100/年左右
  2. **备案授权码**：若有朋友/其他腾讯云账号已有服务器，可尝试生成备案授权码（注意需符合腾讯云规则）
  3. **暂不备案**：演示阶段直接用 EdgeOne「预览」链接（0 成本）

## 3. 备案期间：照常演示（零成本）
EdgeOne Pages 控制台 → wholesale-agent → 项目概览 → 右上「**预览**」→ 生成带 token 链接
- 链接 3 小时有效，适合当场演示
- 无需等备案

## 4. 备案通过后：EdgeOne 加自定义域
EdgeOne Pages 控制台 → wholesale-agent → **设置 / 域名** → 添加自定义域
- 填 `sirkaikai.cn`
- 平台给出 **CNAME 目标值**（以控制台显示为准，形如 `xxx.eo.dnspod.com`）
- 复制该值

## 5. DNS 解析（腾讯云 DNSPod）
腾讯云控制台 → **DNS 解析 DNSPod** → `sirkaikai.cn` → 添加记录
- 类型：`CNAME`
- 主机记录：`@`（或 `www`，按需）
- 记录值：第 4 步 EdgeOne 给的 CNAME 目标
- 保存

## 6. 等待生效 + 验证
- DNS 生效 10 分钟 ~ 数小时
- 浏览器开 `https://sirkaikai.cn` → 应加载 wholesale-agent
- EdgeOne 控制台显示域名「已生效 / 已绑定」

---

## 注意
- 备案免费，但必须本人实名 + 管局审核，**这一步我无法代做**
- CNAME 具体值以 EdgeOne 控制台实时显示为准，不要照抄本文示例
- 备案没下来前，自定义域解析会被拦；先用第 3 步预览链接顶

## 备案通过后我能直接帮你做的
- EdgeOne 侧自定义域添加配置
- 核对 CNAME 与访问是否生效

## 若 my-first-agent 也要绑
同理：在 EdgeOne 给 my-first-agent 项目加自定义域，可用子域（如 `my.sirkaikai.cn`）或另购域名，步骤同 4-6。
